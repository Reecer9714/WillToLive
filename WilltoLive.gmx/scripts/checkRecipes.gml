//checkRecipes(left_up, up, right_up, left, center, right, left_down, down, right_down)
var left_up = argument0, up = argument1,
    right_up = argument2, left = argument3,
    center = argument4, right = argument5,
    left_down = argument6, down = argument7,
    right_down = argument8;
    

